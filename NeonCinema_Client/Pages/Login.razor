@page "/login"
@using NeonCinema_Client.Data
@using NeonCinema_Client.Pages.Client
@using System.IdentityModel.Tokens.Jwt
@layout LayoutClient
@* @inject CustomAuthenticationStateProvider _authenticationStateProvider *@
@inject NavigationManager _navi
@inject Blazored.LocalStorage.ILocalStorageService _localstorage

<body>
    <div class="background-video">
        <video autoplay muted loop id="bg-video">
            <source src="/images/Ngày 24.... trailer - Phim kinh dị - KC_ 22.04.2022.mp4" type="audio/mp4">
        </video>
    </div>
    <div class="login-container">
        <h1>Đăng Nhập</h1>
        <EditForm Model="@loginModel" OnValidSubmit="LoginAccount">
            <DataAnnotationsValidator />
            <ValidationSummary />  
            <div class="input-group">
                <label for="username">Email hoặc số điện thoại</label>
                <InputText id="username" class="form-control rounded" @bind-Value="loginModel.EmailorPhone" required />
            </div>
            <div class="input-group">
                <label for="password">Mật Khẩu</label>
                <InputText id="password" type="password"  class="form-control rounded" @bind-Value="loginModel.Password" required />
            </div>
            <div>
                @if (!string.IsNullOrEmpty(errorMessage))
                {
                    <p class="text-danger">@errorMessage</p>
                }
            </div>
            <div class="d-flex justify-content-center">
                @if (isProcessing)
                {
                    <div class="spinner-border text-primary" role="status">
                        <span class="visually-hidden">Loading...</span>
                    </div>
                }
               else
               {
                    <button type="submit">Đăng Nhập</button>
               }
                
            </div>
            <div class="links">
                <a href="#">Quên Mật Khẩu?</a>
                <a href="#">Đăng ký</a>
            </div>
        </EditForm>
    </div>
</body>





@code {
    private LoginModel loginModel = new LoginModel();
    private bool isProcessing;
    private string errorMessage;
    private string account;
    private string pass;
    HttpClient _http = new HttpClient();


    public async Task LoginAccount()
    {
        _localstorage.ClearAsync();
        isProcessing = true;
        errorMessage = null;

        try
        {
            var login = await _http.PostAsJsonAsync("https://localhost:7211/api/Login/Login", loginModel);
            if (login.IsSuccessStatusCode)
            {
                try
                {
                    
                    
                    var content = await login.Content.ReadAsStringAsync();
                    var token = new JwtSecurityTokenHandler().ReadJwtToken(content);
                    var claim = token.Claims;

                    // // Gọi NotifyUserAuthentication để xác nhận người dùng đã đăng nhập
                    // _authenticationStateProvider.NotifyUserAuthentication(content);

                    var name = claim.FirstOrDefault(c => c.Type == "http://schemas.xmlsoap.org/ws/2005/05/identity/claims/name")?.Value;
                    var nameidentifier = claim.FirstOrDefault(c => c.Type == "http://schemas.xmlsoap.org/ws/2005/05/identity/claims/nameidentifier")?.Value;
                    var mobilephone = claim.FirstOrDefault(c => c.Type == "http://schemas.xmlsoap.org/ws/2005/05/identity/claims/mobilephone").Value;
                    var email = claim.FirstOrDefault(c => c.Type == "http://schemas.xmlsoap.org/ws/2005/05/identity/claims/emailaddress").Value;     
                    var address = claim.FirstOrDefault(c => c.Type == "http://schemas.xmlsoap.org/ws/2005/05/identity/claims/streetaddress").Value;
                    var dateofbirth = claim.FirstOrDefault(c => c.Type == "http://schemas.xmlsoap.org/ws/2005/05/identity/claims/dateofbirth").Value;
                    var gender = claim.FirstOrDefault(c => c.Type == "http://schemas.xmlsoap.org/ws/2005/05/identity/claims/gender").Value;
                    var role = claim.FirstOrDefault(c => c.Type == "http://schemas.microsoft.com/ws/2008/06/identity/claims/role").Value;
                    var images = claim.FirstOrDefault(c => c.Type == "profile_image_url").Value;
                    await _localstorage.SetItemAsync("token", content);
                    await _localstorage.SetItemAsync("userId", nameidentifier.Trim());
                    await _localstorage.SetItemAsync("mobilephone", mobilephone.Trim());
                    await _localstorage.SetItemAsync("email", email.Trim());
                    await _localstorage.SetItemAsync("address", address.Trim());
                    await _localstorage.SetItemAsync("dateofbirth", dateofbirth.Trim());
                    await _localstorage.SetItemAsync("gender", gender.Trim());
                    await _localstorage.SetItemAsync("name", name.Trim());

                    if(role.Equals("Admin"))
                    {
                        _navi.NavigateTo("/list-movie-admin");
                    }
                    else if(role.Equals("Customer"))
                    {
                        _navi.NavigateTo("/");
                    }

                }
                catch (Exception ex)
                {
                    errorMessage = $"Lỗi khi xử lý JSON: {ex.Message}";
                }
            }
            else
            {
                errorMessage = "Thông tin đăng nhập không chính xác" ;
            }
        }
        catch (Exception ex)
        {
            errorMessage = $"Có lỗi xảy ra: {ex.Message}";
        }
        isProcessing = false;
    }

    public class LoginModel
    {
        public string EmailorPhone { get; set; }
        public string Password { get; set; }
    }
}